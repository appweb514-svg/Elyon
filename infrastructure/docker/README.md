# Docker

Images et builds des services (api, worker, web) utilisés par Docker Compose et par Dokploy. Chaque app fournit son propre Dockerfile.

Le lab Raspberry émulé ajoute `player/Dockerfile` (Debian Bookworm, agent +
moteur headless) via `compose.lab.yaml` — voir `docs/lab.md`.