# Contracts

Contrats partagés entre le serveur et les players : schéma de manifeste JSON, spécification OpenAPI de l'API et types TypeScript générés.
Versionnés de manière rétrocompatible. Consommés par `apps/api`, `apps/web` et `player/agent`.